# ??/?? ??? (AI Search Portal)

? PC?? ?? ???? ???? ?? ????????.

## 1) ?? ??
- Node.js 18.x
- Git
- Chrome ?? Edge (Playwright ???)

## 2) ??? ??
```bash
git clone https://github.com/djyalu/ai-search-portal.git
cd ai-search-portal
```

## 3) ??? ??
```bash
cd server
npm install
cd ../client
npm install
```

## 4) ?? ?? ??
`server/.env` ??:
```
PORT=3000
CLIENT_ORIGIN=http://localhost:5173

BROWSER_CHANNEL=chromium
BROWSER_HEADLESS=false
BROWSER_SLOWMO=40
USER_DATA_BASE=user_data_session

NOTION_TOKEN=your_notion_token_here
NOTION_DATABASE_ID=your_notion_database_id_here
```

(??) `client/.env`:
```
VITE_API_URL=http://localhost:3000
```

## 5) Playwright ??? ?? ?? (??)
```bash
cd server
node setup_auth_playwright.js
```
- Perplexity / ChatGPT / Gemini / Claude ??? ?? ? Enter
- ?? ?? ??: `server/<USER_DATA_BASE>_<BROWSER_CHANNEL>`
  ?: `server/user_data_session_chromium`

## 6) ??
Server:
```bash
cd server
npm run dev
```

Client:
```bash
cd client
npm run dev
```

??:
- ?????: `http://localhost:5173`
- ??: `http://localhost:3000`

## 7) ?? ?? ??
- `http://localhost:5173` ??
- `http://localhost:3000/api/history`? 200 ???? ??

## 8) ?? ??
### ?? ?? (EADDRINUSE)
- `server/.env`? `PORT` ??
- `client/.env`? `VITE_API_URL`? ???? ??

### ??? ?? ???
- `node setup_auth_playwright.js`? ?? ??
- ? ????? ??? ?? ? Enter

### Gemini/Claude ?? ??
- ???? ?? ?? ?? ???
- ?? ? ??? ?? ??: `server/playwright-debug-*.html/png`

## 9) ??/?? ??
- `.env` ??? ???? ????.
- `server/user_data_session_*`, `server/history.db`, `server/server.log`? ?? ?????.
